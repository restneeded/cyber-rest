module CyberRest.UI

// (deprecated / intentionally empty)
//
// The main-menu multiplayer controls now live in CyberRestUI.reds. This file exists ONLY to
// overwrite an older build's MainMenu.reds that would otherwise linger in r6/scripts/CyberRest/
// and re-add a stale, broken "Multiplayer" item (extracting a new build adds files but never
// deletes ones the new build dropped). If you did a clean install you can ignore this file.
